@(maxItem: Int)

package xio.nat.error

trait XErrorProjection {

    type _0 = XErrorZero
    @for(i <- 1 to maxItem) {
        type _@{i}[@for(ii <- 1 to i) { @if(ii > 1) { , } T@{ii} }] = @for(ii <- 1 to i) { XErrorPositive[ } XErrorZero @for(ii <- 1 to i) { , T@{ii}] }
    }

}